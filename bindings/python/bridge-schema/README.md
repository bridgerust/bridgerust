# Bridge Schema

High Performance JSON Schema validator for Python

## Installation
```bash
pip install bridge-schema
```

## Usage
```python
from bridge_schema import Validator

validator = Validator('{"type": "string"}')
validate.validate('"hello"')
```
