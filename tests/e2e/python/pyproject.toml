[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "bridgerust-e2e-test"
version = "0.1.0"
description = "BridgeRust E2E Test Package"
requires-python = ">=3.8"

[tool.maturin]
features = ["python"]
module-name = "bridgerust_e2e_test"

