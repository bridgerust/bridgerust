[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "bridgerust-example"
version = "0.1.0"
description = "Comprehensive BridgeRust example"
requires-python = ">=3.8"

[tool.maturin]
features = ["python"]
module-name = "bridgerust_example"

