[package]
name = "embex-basic-usage"
version = "0.1.0"
edition = "2021"

[dependencies]
bridge-embex = { path = "../../crates/embex/client", features = ["qdrant"] }
bridge-embex-core = { path = "../../crates/embex/core" }
tokio = { version = "1.48", features = ["full"] }
anyhow = "1.0.100"
